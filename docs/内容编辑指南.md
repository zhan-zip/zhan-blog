# 内容编辑指南（写给博主自己）

本博客的**文章 / 项目 / 首页 / 服务 / 关于 / 联系 / 头像**都可以直接编辑 Markdown 文件，无需懂代码。改完推送到 GitHub 会自动部署上线。

---

## 〇、GitHub 令牌（进后台发布文章用）

后台 `/admin/` 发文章需要 **GitHub 访问令牌**：

- **令牌明文**：保存在本地文件 `令牌备忘.local.md`（⚠️ 含敏感信息，**不提交到仓库**，只在你自己电脑上可看）
- **当前令牌到期**：**2026-09-10**，到期后需要**新建令牌**（方法见本地备忘：GitHub → Settings → Tokens → Generate new token (classic) → 勾选 `repo` → 生成）
- **填法**：后台「GitHub 连接」→ 粘贴令牌 → 保存并连接
- 🔒 令牌只存你自己浏览器，勿公开；泄露了就去 GitHub 删除重建

---

## 一、写文章

在 `src/content/posts/` 新建一个 `.md` 文件（文件名就是网址的一部分），格式：

```yaml
---
title: 文章标题        # 必填
published: 2026-08-11 # 必填，发布日期
updated: 2026-08-12   # 可选，更新日期
draft: false          # true = 草稿不发布
description: 一句话摘要 # 可选
tags: [教程, 前端]      # 可选，标签数组
category: 分类名        # 可选，分类
---
正文用 Markdown 写，支持代码高亮、公式、提醒框等。
```

保存后，**首页「最新文章」、归档页、搜索**会自动更新，无需改任何代码。

## 二、加项目

在 `src/content/projects/` 新建 `.md`，格式：

```yaml
---
title: 项目名
description: 一句话描述
tech: [Vue, FastAPI]  # 可选，技术栈
url: https://...      # 可选，在线访问
github: https://...   # 可选，GitHub 仓库
status: 建设中         # 可选，状态标签
featured: true        # true = 出现在首页「精选项目」
---
```

## 三、改关于页

编辑 `src/content/spec/about.md`（自我介绍 / 技能 / 经历 / 渠道）。

## 四、改首页 Hero

编辑 `src/content/spec/home.md`：

```yaml
---
title: 首页
heroTitle: 你好，我是一盏            # 首页大标题
heroSubtitle: 一个用于分享开发教程、记录项目经历的个人博客。  # 副标题
ctaContact: 联系我                   # 「联系我」按钮文字
ctaProjects: 看我的项目               # 「看我的项目」按钮文字
---
```

> 首页的「精选项目」来自 `featured: true` 的项目，「最新文章」自动取最新 4 篇。

## 五、改服务页

编辑 `src/content/spec/services.md`（服务方向卡片 + 合作流程）：

```yaml
---
title: 服务
description: 页面顶部一句话
services:
  - name: Web 开发                # 服务卡片标题
    icon: material-symbols:code-rounded
    desc: 服务描述
    items: [响应式页面, 前端交互]   # 服务要点列表
workflow:
  - step: 咨询                     # 合作流程步骤
    desc: 聊聊你的需求与想法
---
```

## 六、改联系页文案

编辑 `src/content/spec/contact.md`：

```yaml
---
title: 联系
subtitle: 页面顶部一句话
---
```

> 邮箱 / 联系方式：改 `src/config.ts` 的 `profileConfig` 和联系页代码里的 `email`。

## 七、分类与标签

分类/标签**不需要单独维护**：写文章时填 `category` 和 `tags` 就会自动出现在侧边栏；删掉所有用某分类的文章，该分类自动消失。

## 八、换头像

1. 把头像图片放到 `src/assets/images/` 目录
2. 打开 `src/config.ts`，修改 `profileConfig.avatar` 的路径，例如：

```ts
avatar: "assets/images/我的头像.png",
```

## 九、提交上线

**最省事**：双击项目根目录的 **`提交上线.bat`** —— 它会自动「拉取远程 → 提交本地所有修改 → 推送到 GitHub」，约 2 分钟线上生效。

手动命令也一样：

```bash
git add -A
git commit -m "内容更新"
git push origin main
```

推送后 GitHub Actions 会自动构建部署，约 2 分钟后线上生效（`https://zhan-zip.github.io/zhan-blog`）。

> 想本地预览效果：`pnpm dev` 后打开 `http://localhost:4321/zhan-blog`。

## 十、后台编辑 vs 本地编辑（同步机制）

⚠️ 两种编辑方式的更新位置不同，别搞混：

| 方式 | 更新位置 | 谁先看到 |
|------|---------|---------|
| **后台**（`/admin/`）写/编辑文章 | GitHub 远程仓库（API 直写） | **线上先更新**（部署约 2 分钟）；**本地要 `git pull` 才能同步** |
| **本地**改 `src/content/` 文件 | 本地文件 | **本地先看到**（dev 热更新）；`git push` 后线上更新 |

**常见疑问**：「后台发布后，本地为什么看不到？」 —— 因为后台是直写远程仓库，本地工作区不会自动同步。想在本地看后台发布的内容，先执行：

```bash
git pull
```

（本地改文件则是反的：改完本地能看到，push 后线上才有。）
