# Zhan-Blog 项目实施计划

> 记录任务的优先级、难度、实现方法与验证方式。按依赖顺序推进。
> 难度：★ 简单 / ★★ 中等 / ★★★ 复杂。优先级：P0 必须先做 / P1 核心 / P2 增强。
> 项目背景与设计详见 `zhan-blog-project-summary.md`。
> 本机已配置：Claude Code + Playwright 浏览器 MCP + 全局 skills（详见 `../TreaWork/claude快速对照表.md`）。下表列出本项目会用到的前端设计与工程类工具，按阶段选用。

## 〇、可用的工具与 Skills（按阶段选用）

### 前端设计三件套 + 浏览器验证（本项目重点）

| 工具/Skill | 用途 | 对应阶段 |
|-----------|------|---------|
| **taste** | 逆向分析参考站设计 DNA（配色/字体/间距/布局），开工时对 `joshcomeau.com`、`brittanychiang.com` 提取设计令牌，作为视觉改造依据 | M3 |
| **frontend-design** | 设计理念指导，拒绝套模板、做出独特设计（配合 taste 的设计 DNA 落地） | M3–M4 |
| **impeccable** | 前端设计增强：`/impeccable audit`（审计）/ `polish`（打磨）/ `craft`（从零设计）/ `critique`（批判评审） | M4、M9 |
| **Playwright 浏览器 MCP** | 真实打开浏览器看渲染效果、截图、测交互、填表验证（本项目各里程碑验证的主力工具） | 各里程碑验证 |

### 工程类

| 工具/Skill | 用途 | 对应阶段 |
|-----------|------|---------|
| `/init` | 生成项目 CLAUDE.md（项目规范，长期记忆） | M0 |
| **codebase-design** | 代码库架构设计（组件/目录/内容模型划分） | M0–M1 |
| **to-tickets** | 把实施计划拆成可执行任务清单 | M0 |
| **research** | 调研不确定技术点（Twikoo 的 HF 部署、Actions 表单触发方式、Pagefind 接入等） | M5–M8 |
| **prototype** | 快速原型验证某个页面/交互效果 | M4（可选） |
| **code-review** | 系统化代码审查 | 每阶段完成 |
| **diagnosing-bugs** | 结构化排查 bug | 出问题时 |
| **simplify** | 代码简化、去冗余 | M9 |
| **security-review** | 安全审查（Secrets / 环境变量是否误入库） | M2、M9 |
| **run** | 启动项目 dev server 并验证 | M1 起 |

> 用法提示：日常让 Claude 自动按需调用即可；若自动触发失效，直接点名（如"用 taste 分析这个站"）最可靠。

## 一、里程碑总览

| 阶段 | 里程碑 | 内容 | 优先级 | 难度 |
|------|--------|------|--------|------|
| A 基础 | M0 | 项目初始化（git / 目录 / 依赖 / fuwari 引入） | P0 | ★ |
| B 上线 | M1 | 本地预览跑通 | P0 | ★ |
| B 上线 | M2 | GitHub 仓库 + Pages 部署上线 | P0 | ★ |
| C 视觉 | M3 | 配色/字体/双模式改造 | P1 | ★★ |
| C 视觉 | M4 | 页面开发（首页/文章/项目/服务/关于/联系） | P1 | ★★★ |
| C 视觉 | M5 | 内容系统（Markdown 写作 + 分类标签 + Pagefind 搜索） | P1 | ★★ |
| D 互动 | M6 | Twikoo 评论接入 | P1 | ★★ |
| D 互动 | M7 | 联系表单 + 邮件订阅（GitHub Actions） | P1 | ★★ |
| E 运营 | M8 | 内容填充 + 数据统计 | P2 | ★★ |
| E 运营 | M9 | 打磨 + 上线运营 | P2 | ★ |

---

## 二、任务明细

### 阶段 A：项目基础（P0）

**A1 · M0 项目初始化** —— 优先级 P0，难度 ★

- **实现**：`git init`；创建目录骨架（`src/content/{posts,projects}/`、`public/`、`.github/workflows/`）；`.gitignore` 排除 `.env`、`node_modules/`、`dist/`；引入 fuwari 模板（clone 或复制其源码）；`pnpm install` 安装依赖
- **验证**：`pnpm dev` 本地能启动，无报错
- **注意**：fuwari 要求 Node ≥ 20、pnpm ≥ 9；安装需联网（梯子已开）

---

### 阶段 B：上线（P0）

**B1 · M1 本地预览** —— 优先级 P0，难度 ★

- **实现**：跑通 fuwari 默认首页 + 一篇示例文章；本地浏览器验证渲染
- **验证**：页面正常渲染、亮/暗模式切换正常
- **注意**：用 Playwright MCP 打开 localhost 查看实际效果

**B2 · M2 GitHub Pages 部署** —— 优先级 P0，难度 ★

- **实现**：`gh repo create zhan-blog`；配置 Actions workflow 构建 + 部署到 Pages（`zhan-zip.github.io/zhan-blog` 或根域名）；仓库 Settings 开启 Pages（GitHub Actions 部署源）
- **验证**：线上首页返回 200，图片/样式正常加载
- **注意**：Pages 部署源需在仓库 Settings 手动开启（GitHub Actions）

---

### 阶段 C：视觉与页面（P1）

**C1 · M3 视觉改造** —— 优先级 P1，难度 ★★

- **实现**：按设计方案改配色（主色 `#4242FA` / 强调 `#E60067` / 装饰 `#63BCE9`，暗色用深藏蓝 `#0F172A` 系）；字体（中文思源黑体系 + Inter + JetBrains Mono）；亮暗默认随系统
- **验证**：亮/暗模式切换正常，配色与设计稿一致；无玻璃拟态残留
- **注意**：颜色统一走 CSS 变量，便于后续调整

**C2 · M4 页面开发** —— 优先级 P1，难度 ★★★

- **实现**：改造 fuwari 并新增页面
  - 首页：Hero（一句话 + 双 CTA）+ 精选项目卡片 + 最新文章 + 页脚联系区
  - 文章列表：分类 / 标签 / 分页
  - 文章详情：目录、代码高亮 + 复制按钮、阅读进度条
  - 项目页：Brittany 式分区（标题 + 描述 + 技术栈 + 链接）
  - 服务页：定位一句 → 三方向卡片 → 合作流程 → 案例 → 表单
  - 关于页：经历时间线 + 技能标签 + 渠道
  - 联系页：表单（占位，M7 接真）+ 邮箱 + 社交
- **验证**：每页渲染正常，滚动渐入 / hover 等交互动效生效
- **注意**：优先完成结构，动效逐步加，避免一次做太满

**C3 · M5 内容系统** —— 优先级 P1，难度 ★★

- **实现**：确认 fuwari 内容 schema（frontmatter 字段）；接入 Pagefind 搜索；写一篇示例教程文章验证「加 .md 文件 → 自动出新页面」流程
- **验证**：搜索能命中文章；新文章无需改代码即可上线
- **注意**：内容 schema 确认后回填到交接文档第八节

---

### 阶段 D：互动（P1）

**D1 · M6 Twikoo 评论** —— 优先级 P1，难度 ★★

- **实现**：在 Hugging Face Space 部署 Twikoo 后端（免费）；配置 `SECURE_DOMAINS=zhan-zip.github.io`；前端接入 Twikoo 评论组件（fuwari 已支持）
- **验证**：本地提交评论能存能显；线上匿名评论可用
- **注意**：**HF 国内访问需实测**，不通则换 Railway / Zeabur 等备选平台
- **注意**：`SECURE_DOMAINS` 与实际访问域名必须一一对应，否则 403

**D2 · M7 联系表单（2026-08-11 方案改为 Web3Forms）** —— 优先级 P1，难度 ★★

- **实现（Web3Forms）**：
  - 前端表单（姓名/邮箱/留言）+「提交中/成功/失败」三态反馈 + `botcheck` 蜜罐
  - POST `api.web3forms.com/submit`，`access_key` 前端持有（公开安全）→ 直发博主邮箱
  - 联系渠道卡片：邮箱「写信（mailto）」+「一键复制」
- **验证**：提交表单能收到邮件（access_key 需在 web3forms.com 注册获取，国内访问需实测）
- **注意**：原 GitHub Actions 方案因「前端无法安全持有 token 触发 Actions」放弃；国内访问 Web3Forms 失败则退回 mailto 组装
- **邮件订阅**：暂缓，后续需要时再设计

---

### 阶段 E：运营（P2）

**E1 · M8 内容填充 + 数据统计** —— 优先级 P2，难度 ★★

- **实现**：录入真实文章 / 项目 / 服务内容（用户提供）；接入 Umami 或百度统计
- **验证**：流量数据能看到来源与页面

**E2 · M9 打磨上线** —— 优先级 P2，难度 ★

- **实现**：性能 / SEO / 分享图（OG）优化；写「如何自己编辑内容」操作说明（用户可读）
- **验证**：PageSpeed 分数达标；按操作说明走一遍写作流程顺畅

---

## 三、依赖关系

```
A1 → B1 → B2
B1 → C1 → C2
C2 → C3（页面骨架就绪后做内容系统）
B2 + C2 → D1（线上地址定后配 Twikoo SECURE_DOMAINS）
C2 → D2（联系页存在后接表单）
C3 → E1
E1 → E2
```

## 四、执行顺序建议

1. **A → B 阶段**依次完成（先让博客能上线）
2. **C 阶段** C1 → C2 → C3（先视觉、再页面、后内容系统）
3. **D 阶段** D1 → D2（评论先于表单，均依赖线上地址定下后配白名单）
4. **E 阶段** E1 → E2

> 建议每完成一个里程碑即做验证（见各任务），用 Playwright MCP 测前端页面，减少人工验证负担。
